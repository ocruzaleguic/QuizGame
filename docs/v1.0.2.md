

🐞 1. CORRECCIÓN DE ERRORES:

    No se encontraron errores en el flujo de la aplicación.
    Hay oportunidades de mejora, sobre todo en el aspecto de la seguridad de la aplicación


⚙️ 2. MEJORAS Y OPTIMIZACIONES:

    2.1. Registro debe validar también contra usuarios semilla

        > Se crearon nuevas funciones en utils para reutilizar el código de doble validacion.
        > Se creó la función getAllUsers() para generalizar la doble validación
        > Se usó la función getAllUsers() para ambos casos: el login, y el registro


    2.2. No guardar la contraseña dentro de loggedUser

        > Se siguió la recomendación tal cual y se añadió const { password, ...safeUser } = foundUser; para un login seguro


    2.3. Proteger las pantallas privadas (Menú, Quiz, Resultados, Settings)

        > Se siguió la recomendación tal cual y se añadió const { password, ...safeUser } = foundUser; para un login seguro
        > Se llamó a la función con un script en las pestañas recomendadas: menu, quiz, quizEnd, settings


    2.4. Centralizar la lógica de Logout en auth.js

        > Se siguió la recomendación. Se añadió la función logout() en auth.js
        > Se importó la función y se usó en el menú


    2.5. Evitar registrar múltiples listeners en el Quiz

        > Se decidió crear la función initQuizPage() para ejecutarla una sola vez
        > El quiz ya no acumula llamadas, evitando el uso de recursos innecesarios

