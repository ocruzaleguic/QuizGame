

✨ 1. NUEVAS FUNCIONALIDADES

    1.1. Retroalimentación visual inmediata al responder

        > Respuestas correctas resaltadas en verde
        > Respuestas incorrectas resaltadas en rojo
        > Feedback visible durante 1.5 segundos antes de avanzar

    1.2. Sistema de gamificación persistente con XP por usuario

        > La pantalla del menú muestra el XP actual
        > Se dió soporte para los usuarios semilla en users.json
        > Cada usuario conserva su XP de forma permanente en el localStorage



🐞 2. CORRECCIÓN DE ERRORES:

    2.1. Fallo al iniciar sesión
    
        > Error:

            Al momento de cerrar sesión, no permitía volver a ingresar a tu usuario. Solamente funcionaba para los usuarios semilla
        
        > Motivo:

            La contraseña del usuario se sobrescribía al sincronizar loggedUser en registeredUsers

        > Solución:

            Evitar usar loggedUser para persistencia y preservar siempre el campo password en syncUser(). loggedUser solo se usa para saber qué usuario está logeado.


⚙️ 3. MEJORAS Y OPTIMIZACIONES:

    3.1. Creación de submitAnswer() para separar lógica

    3.2. Deshabilitación de el botón y radios durante el feedback para evitar múltiples envíos

    3.3. Estructura más ordenada de usuarios. Ahora la persistencia de datos se maneja únicamente en registeredUsers

    3.4. Cambio de div por label en el quiz para poder hacer la selección completa, evitando la frustracion del usuario

    3.5. Ahora se muestra el area seleccionada en el menú y durante el quiz

        > Creación de getSelectedAreaId(), loadSelectedArea(), y showSelectedArea() para facilitar la reutilización de código 


🧹 4. CAMBIOS MENORES:

    4.1. Estilos añadidos para las clases .user-xp , .correct y .incorrect
    
    4.2. Arreglos menores en el css

    4.3. Cambios en los comentarios